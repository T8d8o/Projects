const initial = 2000;
const rate = 10;
const years = 3;
const getValue = (init = 0, rate = 5, time = 0) => {
  //const value = init + (init * (rate / 100) * time);
  //return value;
};
getValue(initial, rate, years);